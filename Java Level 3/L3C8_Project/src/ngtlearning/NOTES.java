package ngtlearning;

public class NOTES {

	/**
	 * URL and URLConnection:::
	 * class URLConnection is the superclass of all classes representing communications link b/w application and a URL
	 * instances of this can be used both to read from and write to the resource referenced by the URL
	 * connection object is created by invoking the openConnection method on a URL
	 *   eg. URL url = new URL("http://www.google.com
	 *       URLConnection urlCon = url.openConnection();
	 * some commonly used methods:
	 * -getHeaderField
	 * -getInputStream
	 * -getOutputStream
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
